import TransactionsClient from "./client";

export const metadata = {
  title: "Transactions Ledger — DominiFunds",
};

export default function TransactionsPage() {
  return <TransactionsClient />;
}
